services:
  - type: web
    name: word-pdf-exports
    env: docker
    # Job state is in memory, so use one worker; threads keep polling and
    # uploads responsive while an export runs in the background.
